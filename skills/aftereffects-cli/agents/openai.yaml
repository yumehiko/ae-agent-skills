interface:
  display_name: "After Effects CLI"
  short_description: "Inspect, snapshot, and edit After Effects scenes"
  default_prompt: "Use $aftereffects-cli to inspect, snapshot, and surgically edit an existing After Effects scene."
