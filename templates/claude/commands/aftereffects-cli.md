Use the `aftereffects-cli` skill for this request.

Prefer direct `ae-cli` commands for surgical edits, inspection, property updates, expression debugging, and small changes to existing human-made After Effects scenes.

User request:

$ARGUMENTS
